<div class="reveal-inner component-inner">
    {{> component this}}
    <div class="reveal-imageboard">
    	<img class="reveal-foreground" src="{{ _reveal._foreground }}">
        <div id="reveal-track">
            <button class="reveal-button">{{ _reveal._button.textLeft }}</button>
        </div>
    	<div class="reveal-overlaycontainer">
    		<div class="reveal-hider">
    			<img class="reveal-background" src="{{ _reveal._background }}">
                {{#if _items}}
                <div class="reveal-hotspots">
                {{#each _items}}
                    <a href="#" tabindex="-1" class="reveal-hotspot {{#if _isVisited}}visited{{/if}}" data-id="{{@index}}" style="top:{{{_top}}}%; left:{{{_left}}}%;" role="button" aria-label="{{pinAlt}}">
                    <div class="icon icon-pin"></div>
                    </a>
                {{/each}}
                </div>
                {{/if}}
    		</div>
    	</div>
    	
    </div>
</div>
